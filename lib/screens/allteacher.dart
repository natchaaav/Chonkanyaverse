// ignore: unused_import
import 'package:flutter/material.dart';

// ignore: camel_case_types
class bigboss extends StatefulWidget {
  const bigboss({super.key});

  @override
  State<bigboss> createState() => _bigbossState();
}

// ignore: camel_case_types
class _bigbossState extends State<bigboss> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      backgroundColor: Colors.indigo.shade800,
      title: const Text('คณะผู้บริหาร'),),);
  }
}
// ignore: camel_case_types
class teachersci extends StatefulWidget {
  const teachersci({super.key});

  @override
  State<teachersci> createState() => _teachersciState();
}

// ignore: camel_case_types
class _teachersciState extends State<teachersci> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      backgroundColor: Colors.indigo.shade800,
      title: const Text('กลุ่มสาระการเรียนรู้วิทยาศาสตร์และเทคโนโลยี'),),);
  }
}

// ignore: camel_case_types
class teachermath extends StatefulWidget {
  const teachermath({super.key});

  @override
  State<teachermath> createState() => _teachermathState();
}

// ignore: camel_case_types
class _teachermathState extends State<teachermath> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      backgroundColor: Colors.indigo.shade800,
      title: const Text('กลุ่มสาระการเรียนรู้คณิตศาสตร์'),),);
  }
}

// ignore: camel_case_types
class teacherthai extends StatefulWidget {
  const teacherthai({super.key});

  @override
  State<teacherthai> createState() => _teacherthaiState();
}

// ignore: camel_case_types
class _teacherthaiState extends State<teacherthai> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      backgroundColor: Colors.indigo.shade800,
      title: const Text('กลุ่มสาระการเรียนรู้ภาษาไทย'),),);
  }
}

// ignore: camel_case_types
class teachereng extends StatefulWidget {
  const teachereng({super.key});

  @override
  State<teachereng> createState() => _teacherengState();
}

// ignore: camel_case_types
class _teacherengState extends State<teachereng> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      backgroundColor: Colors.indigo.shade800,
      title: const Text('กลุ่มสาระการเรียนรู้ภาษาต่างประเทศ'),),);
  }
}

// ignore: camel_case_types
class teacherso extends StatefulWidget {
  const teacherso({super.key});

  @override
  State<teacherso> createState() => _teachersoState();
}

// ignore: camel_case_types
class _teachersoState extends State<teacherso> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      backgroundColor: Colors.indigo.shade800,
      title: const Text('กลุ่มสาระการเรียนรู้สังคมศึกษา'),),);
  }
}

// ignore: camel_case_types
class teacherPE extends StatefulWidget {
  const teacherPE({super.key});

  @override
  State<teacherPE> createState() => _teacherPEState();
}

// ignore: camel_case_types
class _teacherPEState extends State<teacherPE> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      backgroundColor: Colors.indigo.shade800,
      title: const Text('กลุ่มสาระการเรียนรู้สุขศึกษาและพลศึกษา'),),);
  }
}

// ignore: camel_case_types
class teacherart extends StatefulWidget {
  const teacherart({super.key});

  @override
  State<teacherart> createState() => _teacherartState();
}

// ignore: camel_case_types
class _teacherartState extends State<teacherart> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      backgroundColor: Colors.indigo.shade800,
      title: const Text('กลุ่มสาระการเรียนรู้ศิลปะและดนตรี'),),);
  }
}

// ignore: camel_case_types
class teacherhome extends StatefulWidget {
  const teacherhome({super.key});

  @override
  State<teacherhome> createState() => _teacherhomeState();
}

// ignore: camel_case_types
class _teacherhomeState extends State<teacherhome> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      backgroundColor: Colors.indigo.shade800,
      title: const Text('กลุ่มสาระการเรียนรู้การงานอาชีพ'),),);
  }
}